pip install requests python-dotenv python-telegram-bot

git clone https://github.com/penggan00/rss.git  

chmod +x ~/usd/usd.sh  

sudo apt install python3.8-venv
python3 -m venv usd_venv
# 激活虚拟环境
source usd_venv/bin/activate

pip install tushare python-telegram-bot python-dotenv

# 退出虚拟环境
deactivate


crontab -e